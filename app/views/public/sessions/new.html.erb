<div class="container mt-5">
  <div class="row">
    <div class="col text-center">
      <h2>ログイン</h2>
      <h7>rackrackに登録済みの方は、下記からログインして下さい。</h7>
      <h7>※ゲストログインはこちら</h7><br>
      <%= link_to 'ゲストログイン（閲覧用）', guest_sign_in_path, method: :post %>

    </div>
  </div>
  <%= form_with model: @customer, url: customer_session_path do |f| %>
    <br><div class="field text-center">
      <%= f.label :email, "メールアドレス" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div><br>

    <div class="field text-center">
      <%= f.label :password, "パスワード" %><br />
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field text-center">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div><br>
    <% end %>

    <div class="field text-center">
      <%= f.submit "ログイン", class: "btn btn-outline-primary" %>
    </div>
  <% end %>
    <div class="row text-center">
      <div class="col-12"><br>
        <p><h7>登録がお済でない方はこちら</h7></p>
        <%= render "public/shared/links" %>
      </div>
    </div>
</div>
